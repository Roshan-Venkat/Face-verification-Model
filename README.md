# Face-verification-Model

The real purpose of this program is to verify a person with their ID's

The preson need to some up a ID near to their face so that it capture it, and extract the faces from that image and comapare it.

The face matching is done at a thershold of 60% match.
